const config = {
    BASE_URL: process.env.REACT_APP_BASE_URL || 'https://vs598.dei.isep.ipp.pt/api', 
        // Localmente, usa o localhost para testar. Em produção, usa o IP do servidor.   
};

export default config;
